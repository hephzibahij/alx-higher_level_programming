#!/usr/bin/node
// Accessing the first and second arguments from the command line
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Printing the two arguments in the specified format
console.log(`${arg1} is ${arg2}`);
