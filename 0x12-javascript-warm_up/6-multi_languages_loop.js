#!/usr/bin/node
// Array of string
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
// loop to print each lines
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
